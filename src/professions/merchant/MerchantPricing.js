import { ItemRegistry } from '../../core/ItemRegistry.js';import { MerchantReputationManager } from './MerchantReputationManager.js';export class MerchantPricing{constructor(){this.items=new ItemRegistry();this.reputation=new MerchantReputationManager();this.baseBuyFee=.05;this.baseSellFee=.08;}reputationDiscount(u){return Math.min(.40,this.reputation.get(u)/25000)}buyFee(u){return this.baseBuyFee*(1-this.reputationDiscount(u))}sellFee(u){return this.baseSellFee*(1-this.reputationDiscount(u))}calculateBuyPrice(u,id,q){const subtotal=this.items.getBasePrice(id)*q;return Math.round((subtotal+subtotal*this.buyFee(u))*100)/100}calculateSellPrice(u,id,q){const subtotal=this.items.getBasePrice(id)*q;return Math.round(Math.max(0,subtotal-subtotal*this.sellFee(u))*100)/100}}

import path from 'node:path'; import { BLACKSMITH_DATA_DIR } from '../../config/settings.js'; import { JsonRegistry } from '../JsonRegistry.js'; export class BlacksmithStationRegistry extends JsonRegistry{constructor(){super(path.join(BLACKSMITH_DATA_DIR,'stations.json'));} bestForLevel(level){return this.availableForLevel(level).sort((a,b)=>Number(b.required_level)-Number(a.required_level))[0]??null;}}

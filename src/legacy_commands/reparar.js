import { SlashCommandBuilder } from 'discord.js';import { MechanicSystem } from '../professions/mechanic/MechanicSystem.js';export default{data:new SlashCommandBuilder().setName('reparar').setDescription('Repara um equipamento.').addStringOption(o=>o.setName('equipamento').setDescription('Instance ID').setRequired(true)).addStringOption(o=>o.setName('reparo').setDescription('ID do reparo').setRequired(true)),async execute(i){const s=new MechanicSystem();const[ok,msg,r]=s.repair(i.user.id,i.options.getString('equipamento',true),i.options.getString('reparo',true));await i.reply({content:ok?`✅ ${msg} Durabilidade: ${r.durability}/${r.instance.max_durability}`:`❌ ${msg}`,ephemeral:true});}}

import { SlashCommandBuilder } from 'discord.js'; import { IndustryHubService } from '../industrial/IndustryHubService.js'; export default {data:new SlashCommandBuilder().setName('auditarindustria').setDescription('Audita referências da indústria estratégica.'),async execute(i){const r=new IndustryHubService().audit();await i.reply({content:r.ok?'✅ Indústria estratégica consistente.':`⚠️ ${r.issues.length} problema(s):\n${r.issues.slice(0,15).map(x=>`• ${x.type}: ${x.recipe}`).join('\n')}`,ephemeral:true});}};

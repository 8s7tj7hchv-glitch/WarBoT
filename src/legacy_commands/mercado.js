import { SlashCommandBuilder, EmbedBuilder, MessageFlags } from 'discord.js';
import { MarketOrderManager } from '../market/MarketOrderManager.js';
import { MarketPricing } from '../market/MarketPricing.js';
import { ItemRegistry } from '../core/ItemRegistry.js';
const orders=new MarketOrderManager(), pricing=new MarketPricing(), items=new ItemRegistry();
export const data=new SlashCommandBuilder().setName('mercado').setDescription('Mostra o livro de ordens do mercado.').addStringOption(o=>o.setName('item').setDescription('ID do item').setRequired(false));
export async function execute(interaction){const item=interaction.options.getString('item');const sells=orders.listSell(item).slice(0,8),buys=orders.listBuy(item).slice(0,8);const fmt=(o,side)=>`${side} ${items.getEmoji(o.item_id)} **${items.getName(o.item_id)}** • ${o.remaining} un. • $${Number(o.unit_price).toFixed(2)} • \`${o.id.slice(0,8)}\``;const embed=new EmbedBuilder().setTitle('📈 Mercado Avançado').setDescription(item?`Item: **${items.getName(item)}**\nPreço de referência: **$${pricing.marketSummary(item).reference_price.toFixed(2)}**`:'Livro de ordens aberto').addFields({name:'🟢 Compras',value:buys.length?buys.map(o=>fmt(o,'COMPRA')).join('\n'):'Nenhuma ordem.'},{name:'🔴 Vendas',value:sells.length?sells.map(o=>fmt(o,'VENDA')).join('\n'):'Nenhuma ordem.'});await interaction.reply({embeds:[embed],flags: MessageFlags.Ephemeral});}

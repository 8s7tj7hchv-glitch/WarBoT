import { SlashCommandBuilder } from 'discord.js'; import { MilitaryMarketManager } from '../industrial/MilitaryMarketManager.js'; export default {data:new SlashCommandBuilder().setName('venderestrategico').setDescription('Cria venda de equipamento estratégico.').addStringOption(o=>o.setName('item').setDescription('ID').setRequired(true)).addIntegerOption(o=>o.setName('quantidade').setDescription('Quantidade').setMinValue(1).setRequired(true)).addNumberOption(o=>o.setName('preco').setDescription('Preço unitário').setMinValue(0.01).setRequired(true)).addBooleanOption(o=>o.setName('internacional').setDescription('Usar mercado internacional?')),async execute(i){const m=new MilitaryMarketManager(),intl=i.options.getBoolean('internacional')??false;const args=[i.user.id,i.options.getString('item'),i.options.getInteger('quantidade'),i.options.getNumber('preco')];const [ok,msg]=intl?m.createInternationalSell(i.user.id,i.guildId,i.guild?.name??'Servidor',...args.slice(1)):m.createLocalSell(...args);await i.reply({content:`${ok?'✅':'❌'} ${msg}`,ephemeral:true});}};

import { SlashCommandBuilder, MessageFlags } from 'discord.js'; import { MilitaryProductionManager } from '../industrial/MilitaryProductionManager.js'; export default {data:new SlashCommandBuilder().setName('produzirestrategico').setDescription('Produz um item estratégico fictício.').addStringOption(o=>o.setName('receita').setDescription('ID da receita').setRequired(true)).addIntegerOption(o=>o.setName('quantidade').setDescription('Quantidade').setMinValue(1)),async execute(i){const [ok,msg,r]=new MilitaryProductionManager().craft(i.user.id,i.options.getString('receita'),i.options.getInteger('quantidade')??1);await i.reply({content:`${ok?'✅':'❌'} ${msg}${r?`\n📦 ${r.item_id} ×${r.quantity} → ${r.storage}`:''}`,flags: MessageFlags.Ephemeral});}};

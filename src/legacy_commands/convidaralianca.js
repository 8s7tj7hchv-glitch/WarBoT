import { SlashCommandBuilder } from 'discord.js';import { CountryManager } from '../world/CountryManager.js';import { AllianceManager } from '../diplomacy/AllianceManager.js';const countries=new CountryManager(), alliances=new AllianceManager({countries});
export default{data:new SlashCommandBuilder().setName('convidaralianca').setDescription('Convida um país para sua aliança.').addStringOption(o=>o.setName('pais').setDescription('ID do país').setRequired(true)),async execute(i){try{const c=countries.memberCountry(i.user.id);if(!c)throw new Error('Você não pertence a um país.');const a=alliances.countryAlliance(c.id);if(!a)throw new Error('Seu país não pertence a uma aliança.');alliances.invite(a.id,i.options.getString('pais',true),i.user.id);await i.reply({content:'✅ Convite enviado.',ephemeral:true})}catch(e){await i.reply({content:`❌ ${e.message}`,ephemeral:true})}}};

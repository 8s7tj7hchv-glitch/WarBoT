import { SlashCommandBuilder, EmbedBuilder } from 'discord.js';import { BlacksmithSystem } from '../professions/blacksmith/BlacksmithSystem.js';import { EMBED_COLOR } from '../config/settings.js';
export default{data:new SlashCommandBuilder().setName('forjar').setDescription('Produz itens pela profissão Ferreiro.').addStringOption(o=>o.setName('receita').setDescription('ID da receita').setRequired(true)).addIntegerOption(o=>o.setName('quantidade').setDescription('Quantidade de lotes').setMinValue(1).setMaxValue(1000)),async execute(i){const s=new BlacksmithSystem();const[ok,msg,r]=s.forge(i.user.id,i.options.getString('receita',true),i.options.getInteger('quantidade')??1);if(!ok)return i.reply({content:`❌ ${msg}`,ephemeral:true});await i.reply({embeds:[new EmbedBuilder().setColor(EMBED_COLOR).setTitle('🔨 Forja concluída').setDescription(`Receita: **${r.recipe.name}**\nProduzido: **${r.quantity}**\nQualidade: **${r.quality}**\nDestino: **${r.destination}**\nEstação: **${r.station?.name??'Forja básica'}**`)],ephemeral:true});}}

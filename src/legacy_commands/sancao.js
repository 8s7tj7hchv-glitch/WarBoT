import { SlashCommandBuilder } from 'discord.js';import { CountryManager } from '../world/CountryManager.js';import { RestrictionManager } from '../diplomacy/RestrictionManager.js';const countries=new CountryManager(), restrictions=new RestrictionManager({countries});
export default{data:new SlashCommandBuilder().setName('sancao').setDescription('Aplica uma sanção abstrata de jogo.').addStringOption(o=>o.setName('pais').setDescription('ID do país alvo').setRequired(true)).addIntegerOption(o=>o.setName('dias').setDescription('Duração').setMinValue(1).setMaxValue(90)).addStringOption(o=>o.setName('motivo').setDescription('Motivo fictício')),async execute(i){try{const c=countries.memberCountry(i.user.id);if(!c)throw new Error('Você não pertence a um país.');const r=restrictions.impose({issuerCountryId:c.id,targetCountryId:i.options.getString('pais',true),actorId:i.user.id,type:'sanction',category:'all',durationDays:i.options.getInteger('dias')??7,reason:i.options.getString('motivo')??'sanção do jogo'});await i.reply({content:`✅ Sanção aplicada: \`${r.id}\`.`,ephemeral:true})}catch(e){await i.reply({content:`❌ ${e.message}`,ephemeral:true})}}};

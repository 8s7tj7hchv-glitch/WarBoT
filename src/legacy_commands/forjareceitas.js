import { SlashCommandBuilder, EmbedBuilder } from 'discord.js';import { BlacksmithSystem } from '../professions/blacksmith/BlacksmithSystem.js';import { EMBED_COLOR } from '../config/settings.js';export default{data:new SlashCommandBuilder().setName('forjareceitas').setDescription('Lista receitas disponíveis do Ferreiro.'),async execute(i){const s=new BlacksmithSystem(),list=s.availableRecipes(i.user.id);const text=list.slice(0,20).map(r=>`${r.emoji??'🔨'} **${r.name}** • Nv. ${r.required_level}\nID: \`${r.id}\` → ${r.output?.item_id} ×${r.output?.quantity}`).join('\n\n')||'Nenhuma receita disponível.';await i.reply({embeds:[new EmbedBuilder().setColor(EMBED_COLOR).setTitle('🔨 Receitas do Ferreiro').setDescription(text)],ephemeral:true});}}

import { SlashCommandBuilder } from 'discord.js';import { CountryManager } from '../world/CountryManager.js';import { TreatyManager } from '../diplomacy/TreatyManager.js';const countries=new CountryManager(), treaties=new TreatyManager({countries});
export default{data:new SlashCommandBuilder().setName('proportratado').setDescription('Propõe um tratado a outro país.').addStringOption(o=>o.setName('pais').setDescription('ID do país alvo').setRequired(true)).addStringOption(o=>o.setName('tipo').setDescription('Tipo').setRequired(true).addChoices({name:'Não agressão',value:'non_aggression'},{name:'Comércio',value:'trade'},{name:'Defesa mútua',value:'mutual_defense'},{name:'Pesquisa',value:'research'})).addIntegerOption(o=>o.setName('dias').setDescription('Duração em dias').setMinValue(1).setMaxValue(365)),async execute(i){try{const c=countries.memberCountry(i.user.id);if(!c)throw new Error('Você não pertence a um país.');const t=treaties.propose({proposerCountryId:c.id,targetCountryId:i.options.getString('pais',true),actorId:i.user.id,type:i.options.getString('tipo',true),durationDays:i.options.getInteger('dias')??30});await i.reply({content:`✅ Tratado proposto: \`${t.id}\`.`,ephemeral:true})}catch(e){await i.reply({content:`❌ ${e.message}`,ephemeral:true})}}};

import { SlashCommandBuilder, EmbedBuilder, MessageFlags } from 'discord.js';import { TruckingSystem } from '../professions/trucking/TruckingSystem.js';import { EMBED_COLOR } from '../config/settings.js';
export default{data:new SlashCommandBuilder().setName('entregar').setDescription('Conclui um contrato de transporte.').addStringOption(o=>o.setName('contrato').setDescription('ID do contrato').setRequired(true)),async execute(i){const s=new TruckingSystem();const[ok,msg,r]=s.deliver(i.user.id,i.options.getString('contrato',true));if(!ok)return i.reply({content:`❌ ${msg}`,flags: MessageFlags.Ephemeral});await i.reply({embeds:[new EmbedBuilder().setColor(EMBED_COLOR).setTitle('🚚 Entrega concluída').setDescription(`Pagamento: **$ ${Number(r.payment).toFixed(2)}**\nXP: **${r.xp}**\nDesgaste: **${r.wear}**\nDurabilidade restante: **${r.durability}**`)],flags: MessageFlags.Ephemeral});}}

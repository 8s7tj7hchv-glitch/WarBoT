import { SlashCommandBuilder } from 'discord.js';import { CountryManager } from '../world/CountryManager.js';import { AllianceManager } from '../diplomacy/AllianceManager.js';const countries=new CountryManager(), alliances=new AllianceManager({countries});
export default{data:new SlashCommandBuilder().setName('aceitaralianca').setDescription('Aceita um convite de aliança.').addStringOption(o=>o.setName('alianca').setDescription('ID da aliança').setRequired(true)),async execute(i){try{const c=countries.memberCountry(i.user.id);if(!c)throw new Error('Você não pertence a um país.');const a=alliances.accept(i.options.getString('alianca',true),c.id,i.user.id);await i.reply({content:`✅ Seu país entrou em **${a.name}**.`,ephemeral:true})}catch(e){await i.reply({content:`❌ ${e.message}`,ephemeral:true})}}};

import { SlashCommandBuilder } from 'discord.js'; import { HeavyLogisticsManager } from '../industrial/HeavyLogisticsManager.js'; export default {data:new SlashCommandBuilder().setName('aceitarfretepesado').setDescription('Aceita um frete estratégico pesado.').addStringOption(o=>o.setName('id').setDescription('ID do frete').setRequired(true)),async execute(i){const [ok,msg]=new HeavyLogisticsManager().accept(i.user.id,i.options.getString('id'));await i.reply({content:`${ok?'✅':'❌'} ${msg}`,ephemeral:true});}};

import { SlashCommandBuilder } from 'discord.js';
import { CountryManager } from '../world/CountryManager.js';
import { MilitaryAssetManager } from '../military/MilitaryAssetManager.js';
const countries=new CountryManager(),assets=new MilitaryAssetManager({countries});
export default{data:new SlashCommandBuilder().setName('incorporarativo').setDescription('Transfere um equipamento fictício do seu armazenamento estratégico para as forças do país.').addStringOption(o=>o.setName('item').setDescription('ID do item militar fictício').setRequired(true)).addIntegerOption(o=>o.setName('quantidade').setDescription('Quantidade').setMinValue(1).setMaxValue(20).setRequired(false)).addStringOption(o=>o.setName('territorio').setDescription('ID de território controlado').setRequired(false)),async execute(i){try{const c=countries.memberCountry(i.user.id);if(!c)throw new Error('Você não pertence a um país.');if(c.leader_id!==String(i.user.id))throw new Error('Somente o líder pode incorporar ativos nesta fase.');const list=assets.commission({countryId:c.id,userId:i.user.id,itemId:i.options.getString('item'),quantity:i.options.getInteger('quantidade')??1,territoryId:i.options.getString('territorio')});await i.reply({content:`✅ **${list.length}** ativo(s) incorporado(s) às forças de ${c.name}.`,ephemeral:true})}catch(e){await i.reply({content:`❌ ${e.message}`,ephemeral:true})}}};

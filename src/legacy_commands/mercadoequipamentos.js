import { SlashCommandBuilder, EmbedBuilder, MessageFlags } from 'discord.js'; import { MilitaryMarketManager } from '../industrial/MilitaryMarketManager.js'; export default {data:new SlashCommandBuilder().setName('mercadoequipamentos').setDescription('Catálogo de equipamentos estratégicos fictícios.'),async execute(i){const m=new MilitaryMarketManager(),rows=m.listItems().map(x=>`${x.emoji??'📦'} **${x.name}** — $${Number(x.base_price||0).toFixed(2)} — ${x.category}\n\`${x.id}\``).join('\n\n');await i.reply({embeds:[new EmbedBuilder().setTitle('🛡️ Mercado de Equipamentos').setDescription((rows||'Nenhum item.').slice(0,4000))],flags: MessageFlags.Ephemeral});}};

import { SlashCommandBuilder } from 'discord.js';import { MerchantSystem } from '../professions/merchant/MerchantSystem.js';export default{data:new SlashCommandBuilder().setName('comprar').setDescription('Compra do comércio NPC para o estoque comercial.').addStringOption(o=>o.setName('item').setDescription('ID do item').setRequired(true)).addIntegerOption(o=>o.setName('quantidade').setDescription('Quantidade').setRequired(true).setMinValue(1)),async execute(i){const s=new MerchantSystem();const[ok,msg,r]=s.buy(i.user.id,i.options.getString('item',true),i.options.getInteger('quantidade',true));await i.reply({content:ok?`✅ ${r.item_emoji} Comprou ${r.quantity}× ${r.item_name} por $ ${r.price.toFixed(2)}. (+${r.reputation} reputação)`:`❌ ${msg}`,ephemeral:true});}}

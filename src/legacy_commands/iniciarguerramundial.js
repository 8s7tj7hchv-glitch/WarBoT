import { SlashCommandBuilder, MessageFlags } from 'discord.js';
import { CountryManager } from '../world/CountryManager.js';
import { WorldWarManager } from '../global_war/WorldWarManager.js';
const countries=new CountryManager(), wars=new WorldWarManager({countries});
export default{data:new SlashCommandBuilder().setName('iniciarguerramundial').setDescription('Inicia uma guerra mundial de jogo entre dois blocos.').addStringOption(o=>o.setName('pais').setDescription('ID do país adversário').setRequired(true)).addStringOption(o=>o.setName('nome').setDescription('Nome do conflito global').setRequired(false)).addStringOption(o=>o.setName('motivo').setDescription('Motivo fictício').setRequired(false)),async execute(i){try{const mine=countries.memberCountry(i.user.id);if(!mine)throw new Error('Você não pertence a um país.');const w=wars.start({initiatorCountryId:mine.id,targetCountryId:i.options.getString('pais',true),actorId:i.user.id,name:i.options.getString('nome')??'Guerra Mundial',reason:i.options.getString('motivo')??'Escalada internacional'});await i.reply({content:`🌐 Guerra mundial criada: **${w.name}**\n\`${w.id}\`\n${w.side_a.label} × ${w.side_b.label}`,flags: MessageFlags.Ephemeral})}catch(e){await i.reply({content:`❌ ${e.message}`,flags: MessageFlags.Ephemeral})}}};

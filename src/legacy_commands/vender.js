import { SlashCommandBuilder, MessageFlags } from 'discord.js';import { MerchantSystem } from '../professions/merchant/MerchantSystem.js';export default{data:new SlashCommandBuilder().setName('vender').setDescription('Vende itens ao comércio NPC.').addStringOption(o=>o.setName('item').setDescription('ID do item').setRequired(true)).addIntegerOption(o=>o.setName('quantidade').setDescription('Quantidade').setRequired(true).setMinValue(1)),async execute(i){const s=new MerchantSystem();const[ok,msg,r]=s.sell(i.user.id,i.options.getString('item',true),i.options.getInteger('quantidade',true));await i.reply({content:ok?`✅ ${r.item_emoji} Vendeu ${r.quantity}× ${r.item_name} por $ ${r.price.toFixed(2)}. Qualidade ${r.quality}.`:`❌ ${msg}`,flags: MessageFlags.Ephemeral});}}

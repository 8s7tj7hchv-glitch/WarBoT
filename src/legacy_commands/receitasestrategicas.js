import { SlashCommandBuilder, EmbedBuilder, MessageFlags } from 'discord.js'; import { RecipeRegistry } from '../production/RecipeRegistry.js'; export default {data:new SlashCommandBuilder().setName('receitasestrategicas').setDescription('Lista receitas industriais estratégicas fictícias.'),async execute(i){const rows=new RecipeRegistry().listCategory('military').map(r=>`**${r.name}** — \`${r.id}\`
${r.facility} Nv.${r.facility_level||1} • Nível jogador ${r.required_level}`);await i.reply({embeds:[new EmbedBuilder().setTitle('📜 Receitas Estratégicas').setDescription(rows.join('\n\n').slice(0,4000)||'Nenhuma receita.')],flags: MessageFlags.Ephemeral});}};

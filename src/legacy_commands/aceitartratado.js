import { SlashCommandBuilder } from 'discord.js';import { CountryManager } from '../world/CountryManager.js';import { TreatyManager } from '../diplomacy/TreatyManager.js';const countries=new CountryManager(), treaties=new TreatyManager({countries});
export default{data:new SlashCommandBuilder().setName('aceitartratado').setDescription('Aceita uma proposta de tratado.').addStringOption(o=>o.setName('tratado').setDescription('ID do tratado').setRequired(true)),async execute(i){try{const c=countries.memberCountry(i.user.id);if(!c)throw new Error('Você não pertence a um país.');const t=treaties.accept(i.options.getString('tratado',true),c.id,i.user.id);await i.reply({content:`✅ Tratado **${t.type}** ativado.`,ephemeral:true})}catch(e){await i.reply({content:`❌ ${e.message}`,ephemeral:true})}}};

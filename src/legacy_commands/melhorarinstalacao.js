import { SlashCommandBuilder } from 'discord.js'; import { MilitaryFacilityManager } from '../industrial/MilitaryFacilityManager.js'; export default {data:new SlashCommandBuilder().setName('melhorarinstalacao').setDescription('Melhora uma instalação estratégica.').addStringOption(o=>o.setName('id').setDescription('ID da instalação').setRequired(true)),async execute(i){const [ok,msg]=new MilitaryFacilityManager().upgrade(i.user.id,i.options.getString('id'));await i.reply({content:`${ok?'✅':'❌'} ${msg}`,ephemeral:true});}};

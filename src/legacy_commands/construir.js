import { SlashCommandBuilder, EmbedBuilder } from 'discord.js';import { ConstructionSystem } from '../professions/construction/ConstructionSystem.js';import { EMBED_COLOR } from '../config/settings.js';
export default{data:new SlashCommandBuilder().setName('construir').setDescription('Executa um projeto de construção.').addStringOption(o=>o.setName('projeto').setDescription('ID do projeto').setRequired(true)),async execute(i){const s=new ConstructionSystem();const[ok,msg,r]=s.build(i.user.id,i.options.getString('projeto',true));if(!ok)return i.reply({content:`❌ ${msg}`,ephemeral:true});await i.reply({embeds:[new EmbedBuilder().setColor(EMBED_COLOR).setTitle('🏗️ Construção concluída').setDescription(`Projeto: **${r.project.name}**\nNovo nível: **${r.level}**\nQualidade: **${r.quality}**\nCusto: **$ ${Number(r.money_cost).toFixed(2)}**`)],ephemeral:true});}}

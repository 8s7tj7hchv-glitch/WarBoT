import { SlashCommandBuilder, EmbedBuilder } from 'discord.js';import { MiningSystem } from '../professions/mining/MiningSystem.js';import { EMBED_COLOR } from '../config/settings.js';export default{data:new SlashCommandBuilder().setName('zonas').setDescription('Lista zonas de mineração liberadas.'),async execute(i){const s=new MiningSystem(),list=s.availableZones(i.user.id);const text=list.map(z=>`${z.emoji??'⛏️'} **${z.name}** • Nv. ${z.required_level}\nID: \`${z.id}\` • Energia ${z.energy_cost} • CD ${z.cooldown}s`).join('\n\n')||'Nenhuma zona disponível.';await i.reply({embeds:[new EmbedBuilder().setColor(EMBED_COLOR).setTitle('⛏️ Zonas de Mineração').setDescription(text)],ephemeral:true});}}

import { SlashCommandBuilder, EmbedBuilder } from 'discord.js';import { ConstructionSystem } from '../professions/construction/ConstructionSystem.js';import { EMBED_COLOR } from '../config/settings.js';export default{data:new SlashCommandBuilder().setName('projetos').setDescription('Lista projetos de construção disponíveis.'),async execute(i){const s=new ConstructionSystem(),list=s.availableProjects(i.user.id);const text=list.slice(0,20).map(p=>`${p.emoji??'🏗️'} **${p.name}** • Nv. ${p.required_level}\nID: \`${p.id}\` • $ ${Number(p.money_cost??0).toFixed(2)}`).join('\n\n')||'Nenhum projeto disponível.';await i.reply({embeds:[new EmbedBuilder().setColor(EMBED_COLOR).setTitle('🏗️ Projetos').setDescription(text)],ephemeral:true});}}

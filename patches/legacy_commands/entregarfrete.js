import { SlashCommandBuilder, MessageFlags } from 'discord.js'; import { CountryManager } from '../world/CountryManager.js'; import { WorldWarManager } from '../global_war/WorldWarManager.js'; import { proMissionManager } from '../pro/ProMissionManager.js'; import { LogisticsManager } from '../market/LogisticsManager.js'; const logistics=new LogisticsManager(); const countries=new CountryManager(), wars=new WorldWarManager({countries});
export const data=new SlashCommandBuilder().setName('entregarfrete').setDescription('Conclui a entrega de um frete P2P.').addStringOption(o=>o.setName('id').setDescription('ID do frete').setRequired(true));
export async function execute(interaction){const [ok,msg]=logistics.deliver(interaction.user.id,interaction.options.getString('id'));if(ok){const c=countries.memberCountry(interaction.user.id);if(c&&wars.activeForCountry(c.id))proMissionManager.record(interaction.user.id,'global_logistics_delivery',1);}await interaction.reply({content:`${ok?'✅':'❌'} ${msg}`,flags: MessageFlags.Ephemeral});}
